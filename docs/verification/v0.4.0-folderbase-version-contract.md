# v0.4.0 Folderbase Version contract verification

Folderbase Core v0.4.0 releases `folderbase-version-v1`: a bounded, portable
full-state artifact with an independently specified canonical digest, a closed
public schema, positive and negative conformance vectors, and a Rust runtime
implementation.

The repository/tag source archive is the normative cross-language distribution.
The `folderbase-core` and `folderbase-cli` Cargo packages are the runtime and CLI
packages; they intentionally do not claim to contain the workspace-level schema,
corpus, or independent reference encoder.

## Exact transition base

The release transition starts from accepted contract merge
`de760987af058740e2d997923f71a56eb01140d2`, after its post-main CI passed.

## RED evidence

Before the release edit:

- the Cargo metadata assertion for Core and CLI v0.4.0 plus the CLI's Core
  `^0.4.0` dependency returned `false`;
- `protocol/releases/0.4/folderbase-version-v1.json` did not exist;
- `protocol/releases/0.4/folderbase-version-v1.candidate.json` still existed with
  `status: candidate`; and
- ADR-0004 was still Proposed.

These failures establish the release-state seam independently of the already
green feature contract.

## GREEN evidence

The release transition passed:

```text
cargo metadata --format-version 1 --no-deps | jq ...                 true
jq ... protocol/schemas/0.4/folderbase-version.schema.json          true
node scripts/verify-folderbase-version-distribution.mjs             32 files
node scripts/verify-folderbase-version-digest-vectors.mjs           passed
node scripts/verify-reorganization-digest-vector.mjs                passed
cargo fmt --all -- --check                                          passed
cargo clippy --workspace --all-targets --all-features --locked
  -- -D warnings                                                    passed
RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features
  --no-deps --locked                                                passed
cargo test --workspace --all-features --locked                      497 passed;
                                                                   3 ignored
bash scripts/test-package-install.sh                                passed
bash scripts/check-ci-policy.sh                                     passed
bash scripts/check-public-eclipse.sh                                passed
git diff --check                                                    passed
```

The distribution remains exactly 32 files: the exact 25-file conformance tree
plus seven required non-conformance contract files. Its omission and same-count
replacement sensitivity checks pass. A mutation test that temporarily restored a
candidate manifest failed with `candidate release manifest must not remain after
release`; removing that counterexample restored GREEN.

The independent digest evidence remains:

```text
minimal-restorable-v1
8752435e37d1e41fd3de30e6dfb536c67571ccb22cd6b8de359b03707da76f51

fidelity-and-lifecycle-v1
5e266f43ddcf18290f1ab7766533e2388bd8605b3b6a5d1e6ff455d824f04e0f

reorganization scope
d1593f60460548508985a2f8c712a91e0505bdd1fc0fe644a02b528a02e09b4a

reorganization plan
8fabf5479662ed25e177455c692e27ec6649e94a7ee3bb08e6f24a75728e99b2
```

The package gate packaged Core v0.4.0 as 48 files and CLI v0.4.0 as 10 files,
compiled and tested both extracted packages, installed the optimized CLI into a
fresh root, and reported:

```text
folderbase 0.4.0
Extracted Cargo packages are self-contained and testable.
```

## Accepted contract evidence

- contract PR: [#22](https://github.com/chalkagents/folderbase/pull/22)
- independent final GO commit:
  `ba9349217a1654db6ac8af0fc460c488a2e69903`, with no P0–P2 findings
- PR CI:
  [run 30470638866](https://github.com/chalkagents/folderbase/actions/runs/30470638866)
- accepted contract merge:
  `de760987af058740e2d997923f71a56eb01140d2`
- post-main CI:
  [run 30470974909](https://github.com/chalkagents/folderbase/actions/runs/30470974909)

## Publication evidence

The release transition is locally verified, but publication has not happened:

- release PR: pending
- release merge commit: pending
- annotated tag object: pending
- `v0.4.0^{}` peel: pending
- GitHub release: pending
- independent public-tag install proof: pending

No release PR, tag, GitHub release, crates.io publication, Cloud authorization,
live sync, or hosted-byte guarantee is claimed by this evidence.
