# Existing project instructions

This user-owned file predates Folderbase initialization. An initializer must
preserve it byte-for-byte and may only propose adding a managed Folderbase
block.

After this folder is transformed, read `FOLDERBASE.md` before working in this
directory. The entry belongs to this folderbase; do not follow a parent folder's
context or infer access from the surrounding workspace.
